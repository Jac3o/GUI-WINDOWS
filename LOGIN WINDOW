app = App(title="LOGIN FUNCTION", height = 400) #Title of app set to 400 height
text_blank = Text(app, text=" ")
text_blank = Text(app, text=" Welcome to BeanBrew ") #title of the review site
text_blank = Text(app, text=" ")
text = Text(app, text= "Enter Username") #code prompting username entry
user_textbox = TextBox(app)
text_blank = Text(app, text=" ")
text = Text(app, text= "Enter Password") #code promting password entry
PW_textbox = TextBox(app, hide_text=True)
#
text_blank = Text(app, text=" ") #text boxes to enter username and password
text_blank = Text(app, text=" ")
#open_button = PushButton(app, text="Login", command=login_user) # button on app, main window
#open_button = PushButton(app, text="Sign Up", command=login_user) # button on app, main window
buttons_box = Box(app)   #####added box for buttons
buttons_box_exit = Box(app)
login_button = PushButton(buttons_box, text="Login", command=login_user, align ="left")   # button to add new session
Signup_button = PushButton(buttons_box, text="Sign Up", command=open_signup, align = "left") #######align save new session
exit_button = PushButton(buttons_box_exit, text="Exit", command=exit_routine, align="left")   # buttontoexit
app.bg = "white"
text_blank = Text(app, text=" ")
